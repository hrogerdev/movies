<h1>
<%= @list.name %>
</h1>

<ul>
  <% @list.movies.each do |movie| %>
    <li>
      <%= movie.title %>
      <%= link_to "X", bookmark_path(@list.bookmarks.find_by(movie: movie)), method: :delete %>
    </li>
  <% end %>
</ul>

<%= link_to 'ajouter un film', new_list_bookmark_path(@list) %>
